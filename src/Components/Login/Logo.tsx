import React from 'react';

const Logo: React.FC = () => {
  return (
    <img
      src="images/logo.png"
      className="object-contain self-center"
      alt="Logo"
    />
  );
};

export default Logo;